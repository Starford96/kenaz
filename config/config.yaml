app:
  log_level: INFO
  http:
    port: 8080

vault:
  path: ${VAULT_PATH:-./vault}

sqlite:
  path: ${SQLITE_PATH:-./kenaz.db}

auth:
  mode: ${AUTH_MODE:-disabled}
  token: ${AUTH_TOKEN:-}

frontend:
  enabled: ${FRONTEND_ENABLED:-true}
  dist_path: ${FRONTEND_DIST_PATH:-./frontend/dist}
